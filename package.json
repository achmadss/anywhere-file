{
  "name": "anywhere-file",
  "private": true,
  "type": "module",
  "workspaces": ["ui", "console", "dashboard"],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "lint": "oxlint --deny-warnings ui console dashboard"
  },
  "devDependencies": {
    "oxlint": "^1.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=26"
  }
}
