// Command agent runs on the customer's PC. It will hold the device key, announce itself on
// the LAN, and expose registered local apps through a gateway. Only the build exists yet.
package main

import "fmt"

func main() {
	fmt.Println("anywhere-file agent: nothing to run yet")
}
