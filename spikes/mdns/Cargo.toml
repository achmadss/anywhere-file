# Throwaway spike crate for issue #3. Its own [workspace] keeps it out of the root
# `cargo build --workspace`.
[workspace]

[package]
name = "rfm-mdns-spike"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
mdns-sd = "0.21"
socket2 = { version = "0.6", features = ["all"] }
